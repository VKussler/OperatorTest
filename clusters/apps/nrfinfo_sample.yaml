{
  "slice_name": "{{slice_name}}",
  "slice_description": "{{slice_description}}",
  "network_functions_configuration": [
    {
      "network_function": "NRF",
      "configuration_intent": "Configure NRF for slice {{slice_name}} with SST {{sst}} and SD {{sd}}"
    },
    {
      "network_function": "NSSF",
      "configuration_intent": "Configure NSSF for slice {{slice_name}} with SST {{sst}} and SD {{sd}}"
    },
    {
      "network_function": "PCF",
      "configuration_intent": "Configure PCF for slice with SST {{sst}} and SD {{sd}}"
    },
    {
      "network_function": "OAI DB",
      "configuration_intent": "Configure OAI DB for slice {{slice_name}} with SST {{sst}} and SD {{sd}}"
    }
  ]
}