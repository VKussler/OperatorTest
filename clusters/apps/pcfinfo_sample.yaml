{
  "sliceConfiguration": {
    "nrfConfiguration": {
      "intent": "Configure NRF with slice name {{slice_name}} in sBIFQDN field for slice {{slice_name}} with SST {{sst}} and SD {{sd}}."
    },
    "nssfConfiguration": {
      "intent": "Configure NSSF with cNSIIdList containing slice name {{slice_name}} for slice {{slice_name}} with SST {{sst}} and SD {{sd}}."
    },
    "pcfConfiguration": {
      "intent": "Configure PCF with PLMNInfoList containing slice with SST {{sst}} and SD {{sd}} for slice {{slice_name}}."
    },
    "oaiDbConfiguration": {
      "intent": "Configure OAI DB SessionManagementSubscriptionData containing slice {{slice_name}} with SST {{sst}} and SD {{sd}}."
    }
  },
  "cmdbData": {
    "intent": "Read the Core-SSN-NRM.yaml file from the objects directory."
  }
}