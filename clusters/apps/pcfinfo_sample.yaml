{
  "slice_configuration": {
    "network_slice_name": "{{network_slice_name}}",
    "slice_type": "{{slice_type}}",
    "service_type": "{{service_type}}",
    "expected_ue_count": "{{expected_ue_count}}",
    "coverage_area": "{{coverage_area}}",
    "guaranteed_bandwidth": "{{guaranteed_bandwidth}}",
    "latency": "{{latency}}",
    "reliability": "{{reliability}}",
    "mobility": "{{mobility}}",
    "security_level": "{{security_level}}",
    "resource_allocation": "{{resource_allocation}}",
    "priority": "{{priority}}",
    "network_functions": [
      {
        "name": "AMF",
        "capacity": "{{amf_capacity}}",
        "location": "{{amf_location}}"
      },
      {
        "name": "SMF",
        "capacity": "{{smf_capacity}}",
        "location": "{{smf_location}}"
      },
      {
        "name": "UPF",
        "capacity": "{{upf_capacity}}",
        "location": "{{upf_location}}"
      },
      {
        "name": "NRF",
        "capacity": "{{nrf_capacity}}",
        "location": "{{nrf_location}}"
      },
      {
        "name": "NSSF",
        "capacity": "{{nssf_capacity}}",
        "location": "{{nssf_location}}"
      },
      {
        "name": "PCF",
        "capacity": "{{pcf_capacity}}",
        "location": "{{pcf_location}}"
      }
    ],
    "oai_db_parameters": {
      "db_host": "{{db_host}}",
      "db_port": "{{db_port}}",
      "db_name": "{{db_name}}",
      "db_user": "{{db_user}}",
      "db_password": "{{db_password}}"
    }
  }
}