{
  "sliceConfiguration": {
    "sliceName": "{{sliceName}}",
    "sliceServices": [
      {
        "serviceName": "videoStreaming",
        "qos": "guaranteed",
        "bandwidth": "10 Mbps"
      },
      {
        "serviceName": "voiceCommunication",
        "qos": "prioritized",
        "bandwidth": "5 Mbps"
      }
    ],
    "networkFunctions": [
      {
        "nfType": "AMF",
        "nfProfile": "profile1"
      },
      {
        "nfType": "SMF",
        "nfProfile": "profile2"
      }
    ]
  },
  "nrfConfiguration": {
    "sBIFQDN": "{{sliceName}}.nrf.example.com"
  },
  "nssfConfiguration": {
    "cNSIIdList": [
      "{{sliceName}}"
    ]
  },
  "pcfConfiguration": {
    "pLMNInfoList": [
      {
        "sst": "{{sst}}",
        "sd": "{{sd}}"
      }
    ]
  },
  "oaiDbConfiguration": {
    "sessionManagementSubscriptionData": {
      "subscribedSnssai": {
        "sst": "{{sst}}",
        "sd": "{{sd}}"
      }
    }
  }
}